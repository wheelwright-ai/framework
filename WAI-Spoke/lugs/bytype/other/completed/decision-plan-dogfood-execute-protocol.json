{
  "id": "decision-plan-dogfood-execute-protocol",
  "i": "decision-plan-dogfood-execute-protocol",
  "ty": "core-protocol",
  "t": "Multi-file changes require Plan → Epic → Dogfood → Execute sequence",
  "s": "o",
  "created_at": "2026-03-14T20:30:00Z",
  "updated_at": "2026-03-15T00:55:00Z",
  "updated_by": "Claude Sonnet 4.6",
  "wakeup_visibility": true,
  "protocol_active": true,
  "_behavior_directive": {
    "what_this_is": "A working agreement governing how the framework AI handles any change touching 2+ files or requiring 6+ steps. The sequence is non-negotiable and must not be collapsed even in YOLO mode. Absorbs and supersedes the workflow section of wai-complexity-advisor.md.",
    "what_this_is_NOT": "Does not apply to single-file edits, lug authoring, or read-only operations. Does not prohibit YOLO mode — YOLO activates AFTER user approval, not before. See exception below.",
    "trigger": "Same threshold as complexity-advisor: 2+ files affected OR 6+ steps required. WAI utility commands (wakeup, closeout, status, etc.) are always exempt.",
    "sequence": {
      "1_plan": "Produce a written plan. State: what changes, why, what is NOT changing.",
      "2_epic_lug": "Write the plan as an epic lug in WAI-Lugs.jsonl. Tell the user the lug ID. Do not proceed until written.",
      "3_dogfood": "Launch a naive subagent — no WAI framework docs, no skills loaded. Give it exactly: (a) the full text of the epic lug, (b) the current full content of every file the plan intends to modify. The subagent must: summarize what it understands the plan to do, identify ambiguity or missing context, flag anything that would cause confusion or harm. Report subagent findings verbatim to user. File names alone are not sufficient — the subagent needs actual file content to reason about impact.",
      "4_iterate": "If subagent found issues: revise the epic lug and rerun dogfood. Append each iteration result to the lug `review_log`. Continue until subagent can describe the plan accurately and execute without confusion.",
      "5_user_approval": "Present revised plan to user. Wait for explicit approval (Green Light or equivalent). If user rejects: record the rejection reason and any concerns in `review_log`, loop back to step 1 with user feedback. Do NOT proceed without explicit approval.",
      "6_execute": "Execute. YOLO mode is now active for the approved scope only.",
      "7_record": "After execution, append a final entry to `review_log`: what was executed, any deviations from plan, whether outcome matched. Close the epic lug."
    },
    "why": "Prevents scope creep, silent divergence, and unreviewed execution. The dogfood subagent catches ambiguity the authoring agent normalizes over. A plan a naive agent cannot follow is a plan that will fail across sessions and models.",
    "failure_mode_this_prevents": "Agent skips plan → executes directly → changes are correct but ungoverned, hard to audit, and set a precedent for skipping governance on larger changes.",
    "exception": "Truly atomic single-step changes (rename one variable, fix one typo, add one lug) skip steps 2-4 but still need user acknowledgment. Also exempt: independent parallel changes where every file receives the same class of operation (e.g. removing the same obsolete line from N files) — these are a single logical unit even if they touch N files. When in doubt, run the protocol.",
    "relationship_to_complexity_advisor": "The complexity-advisor skill detects trigger conditions (2+ files, 6+ steps) and surfaces the planning gate. This lug defines what happens next: plan becomes an epic lug (step 2), gets dogfooded (step 3), executes only after explicit user approval (step 5). The complexity-advisor's bare 'PLAN ACCEPTED → proceed' shorthand is overridden by this protocol — the epic lug and dogfood are not optional."
  },
  "review_log": [
    {
      "date": "2026-03-15T00:55:00Z",
      "action": "protocol_review",
      "agent": "Claude Sonnet 4.6",
      "findings": "Initial review. Issues identified: (1) dogfood inputs ambiguous — file names not enough, need content; (2) no rejection path or activity log; (3) exception too narrow, excluded independent parallel changes; (4) misaligned with complexity-advisor (predates it); (5) ty=signal with s=open causes incorrect open-task counting.",
      "changes_made": "Updated dogfood step to specify full file content required. Added rejection loop to step 5. Added review_log field. Expanded exception clause. Added relationship_to_complexity_advisor. Changed ty from signal to core-protocol. Added protocol_active flag.",
      "open_questions": [],
      "resolved": {
        "closeout_restart": "Not a protocol step. Closeout happens whenever context budget requires it — the lug is progressed like any epic. No step 7 needed."
      }
    }
  ]
}