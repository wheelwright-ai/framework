# Teaching: Signal — Closeout now automatically distributes new signals as teachi...

**Type:** signal
**safe_to_auto_adopt:** true

---

## What This Teaching Does

Appends a high-impact signal to `WAI-Signals.jsonl` on this spoke.

Before appending: check if a signal with the same `timestamp` and `signal` text already exists in `WAI-Signals.jsonl`. If it does, skip — do not duplicate.

## Embedded Signal

```json
{
  "timestamp": "2026-03-15T01:30:00Z",
  "session_id": "session-20260315-0029",
  "signal": "Closeout now automatically distributes new signals as teaching files (Step 9b). old_last_closeout captured at procedure start before Step 5 overwrites it. Condition: hub connected + signals newer than old_last_closeout. Idempotency guard on receiving spoke. Full /wai-teach remains manual for template distribution. Teaching file: closeout-absorbs-signal-teach-v1.md.teaching.",
  "impact": 8,
  "rationale": "Removes the last manual step from the standard session flow. Signal distribution is now automatic at every closeout with zero extra commands.",
  "by": "Claude Sonnet 4.6"
}
```

## Post-Completion

Move this file to `WAI-Spoke/seed/ingest/processed/`.
