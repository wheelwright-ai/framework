# Teaching: Signal — Dogfood subagent pattern validated: a naive agent reviewing ...

**Type:** signal
**safe_to_auto_adopt:** true

---

## What This Teaching Does

Appends a high-impact signal to `WAI-Signals.jsonl` on this spoke.

Before appending: check if a signal with the same `timestamp` and `signal` text already exists in `WAI-Signals.jsonl`. If it does, skip — do not duplicate.

## Embedded Signal

```json
{
  "timestamp": "2026-03-14T20:00:00Z",
  "session_id": "session-20260314-200000",
  "signal": "Dogfood subagent pattern validated: a naive agent reviewing protocol changes caught 4 critical implementation bugs (path mismatch, undefined build process, missing directory creation, unimplementable HMAC) that the authoring agent normalized over. Plan\u2192Epic\u2192Dogfood\u2192Execute is now a mandatory protocol for multi-file changes.",
  "impact": 9,
  "rationale": "Meta-protocol that improves quality of all future framework changes. The dogfood subagent acts as a forcing function for self-contained, unambiguous protocol documentation.",
  "by": "Claude Sonnet 4.6"
}
```

## Post-Completion

Move this file to `WAI-Spoke/seed/ingest/processed/`.
