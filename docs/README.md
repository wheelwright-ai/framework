# Docs

Public framework documentation.

This directory is the new public docs surface. It currently mirrors the surviving
content from `framework/docs/` while the repo is being remodeled.

Key areas:

- `start-here/`
- `setup/`
- `spokes/`
- `hub/`
- `skills/`
- `reference/`

During the transition, some internal generators and legacy references still point
at `framework/docs/`. Keep both until those callers are updated and verified.
