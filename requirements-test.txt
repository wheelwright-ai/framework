# Integration Test Dependencies for Wheelwright Framework

# Terminal automation (CLI equivalent of Selenium)
pexpect>=4.8.0

# Test framework
pytest>=7.0.0
pytest-timeout>=2.1.0

# Existing runtime dependencies (for reference)
# These are already in requirements.txt:
# requests>=2.28.0
# aiohttp>=3.8.0
# pyyaml>=6.0
